<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Payment Receipt</title>

    <style>
      body {
        font-family: Arial, Helvetica, sans-serif;
        background: #f7f7f7;
        margin: 0;
        padding: 0;
      }

      .container {
        max-width: 600px;
        margin: 30px auto;
        background: #ffffff;
        border-radius: 6px;
        border: 1px solid #eee;
        overflow: hidden;
      }

      .header {
        background: #081630; /* Dark blue from your image */
        padding: 25px;
        text-align: center;
        color: #ffffff;
        font-size: 24px;
        font-weight: bold;
      }

      .content {
        padding: 30px;
        color: #333333;
        font-size: 15px;
        line-height: 1.6;
      }

      .highlight-box {
        background: #ff8c00; /* Orange from template */
        color: #ffffff;
        width: 80%;
        margin: 20px auto;
        padding: 15px 0;
        font-size: 26px;
        font-weight: bold;
        text-align: center;
        border-radius: 6px;
      }

      table {
        width: 100%;
        margin-top: 25px;
        border-collapse: collapse;
      }

      table td {
        padding: 8px 0;
      }

      .total {
        font-weight: bold;
        font-size: 18px;
      }

      .footer {
        padding: 20px;
        text-align: center;
        font-size: 12px;
        color: #555;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <!-- Header -->
      <div class="header">Payment Receipt</div>

      <!-- Content -->
      <div class="content">
        <p>Hello <strong><%= firstName %></strong>,</p>

        <p>
          Thank you for your payment. Below is the receipt for your recent
          transaction.
        </p>

        <!-- Highlight Box for Status -->
        <div class="highlight-box">PAYMENT SUCCESSFUL</div>

        <!-- Payment Details -->
        <table>
          <tr>
            <td>Transaction ID:</td>
            <td><strong><%= paymentId %></strong></td>
          </tr>
          <tr>
            <td>Amount Paid:</td>
            <td><strong><%= amount %></strong></td>
          </tr>

          <tr>
            <td>Date:</td>
            <td><%= new Date().toLocaleDateString() %></td>
          </tr>
        </table>

        <p>
          If you have any questions, feel free to contact our customer support.
        </p>
      </div>

      <!-- Footer -->
      <div class="footer">
        © <%= new Date().getFullYear() %> Bravixo. All rights reserved.
      </div>
    </div>
  </body>
</html>
