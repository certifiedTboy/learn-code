const CourseCheckedItem = ({
  children,
  checked = false,
}: {
  children: React.ReactNode;
  checked?: boolean;
}) => {
  // This component is used within AccordionItem for checklist items
  return null; // Rendered by parent AccordionItem
};

export default CourseCheckedItem;
